name: reflection-connection-env
channels:
  - conda-forge
dependencies:
  - python=3.10.*
  - pip

  - pip:
      - jupyter
      - pyyaml
      - tqdm
      - numpy
      - pandas
      - scikit-learn
      - opencv-python
      - torch
      - torchvision
      - transformers
      - pytorch-lightning
      - faiss-cpu
      - wandb
      - plotly
      - matplotlib