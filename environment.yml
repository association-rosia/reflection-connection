name: reflection-connection-env
channels:
  - conda-forge
dependencies:
  - python=3.10.*
  - faiss-gpu
  - pip

  - pip:
      - jupyter
      - pyyaml
      - tqdm
      - opencv-python
      - numpy
      - pandas
      - scikit-learn
      - torch
      - torchvision
      - transformers
      - pytorch-lightning
      - wandb
      - matplotlib
      - plotly